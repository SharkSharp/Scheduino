#include "MaxHeap.h"
#include "SleepProcess.h"
#include "TesteProcess.h"
#include "Pack.h"
#include <stdio.h>
#include "ParamsHandler.h"



int main() 
{
    
    //ProcessHandler pHandler(15);
    //pHandler.AddProcess(new SleepProcess(&pHandler, true));
    //pHandler.AddProcess(new TesteProcess(&pHandler, 1, 5, true, (char*)"TesteProcess1"));
    //pHandler.AddProcess(new TesteProcess(&pHandler, 10, 20, true, (char*)"TesteProcess2"));
    //pHandler.AddProcess(new TesteProcess(&pHandler, 30, 35, true, (char*)"TesteProcess3"));
    //pHandler.AddProcess(new TesteProcess(&pHandler, 40, 50, true, (char*)"TesteProcess4"));
    //pHandler.AddProcess(new TesteProcess(&pHandler, 51, 99, true, (char*)"TesteProcess5"));
    //pHandler.Execute();
    
    
    //byte initBuffer[20];
    
    //Pack<ControlHeader>* pack = new Pack<ControlHeader>(initBuffer);
    //Pack<VoiceHeader>* pack2 = new Pack<VoiceHeader>(initBuffer);
    
    
  
}